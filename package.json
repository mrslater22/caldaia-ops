{
  "name": "caldaia-ops",
  "private": true,
  "workspaces": [
    "apps/boilerops"
  ],
  "scripts": {
    "dev": "npm run dev -w boilerops",
    "build": "npm run build -w boilerops",
    "start": "npm run start -w boilerops",
    "lint": "npm run lint -w boilerops"
  }
}
