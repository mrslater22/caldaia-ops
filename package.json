{
  "name": "caldaia-ops",
  "private": true,
  "scripts": {
    "dev": "npm run dev --prefix apps/portal",
    "build": "npm run build --prefix apps/portal",
    "start": "npm run start --prefix apps/portal",
    "lint": "npm run lint --prefix apps/portal"
  }
}
